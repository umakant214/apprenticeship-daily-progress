import React from "react";
import Section from "./Section";
const Layout = ({ layout }) => {
  //   console.log(layout);
  return (
    <>
      <Section section={layout} />
    </>
  );
};

export default Layout;
