#include <stdio.h>
void main()
{
    int year;
    printf("Enter Year :");
    scanf("%d", &year);

    if (year > 2000)
    {
        printf("Year is reater than 2000");
    }
    else
    {
        printf("Year is reater than 2000");
    }
}